#ifndef GLOBALS_H_
#define GLOBALS_H_

#define ARRAY_LEN(arr)  (sizeof(arr) / sizeof(arr[0]))
#define MAX(a, b)       ((a) > (b) ? (a) : (b))
#define MIN(a, b)       ((a) < (b) ? (a) : (b))

#endif /* GLOBALS_H_ */
